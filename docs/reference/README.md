# Reference material

Drop competitive-analysis source material here (e.g. exported localization
tables) so the parity checklist in ../WIDGY-PARITY.md can be reconciled
against something verifiable rather than memory.

Feature names and capability lists only — never third-party code or assets.
